using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RememberNUseDifScript : MonoBehaviour
{
    public static SwitchDifSelectionScript.whichactive currentactive;
    // Start is called before the first frame update
    void Start()
    {
        currentactive = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
